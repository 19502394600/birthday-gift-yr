# 生日网站素材放置说明

当前主站入口是 `index.html`，正式配置文件是：

```text
src/data/story-config.js
```

## 1. 密码和开场

- 当前密码：`20060907`
- 当前称呼：`小杨同学`
- 开场文字：`小杨同学，生日快乐！`

输入密码后会进入流星雨开头。开场文字全部显示完，按钮才会出现，按钮文案是“开启礼物”。

## 2. 阶段背景音乐

抖音短链不能直接作为稳定的 `<audio>` 文件地址使用，所以这里做成本地音频槽位：

```text
assets/music/countdown-background.m4a
assets/music/main-background.m4a
assets/reference/main-background-video.mp4
```

- `countdown-background.m4a`：倒计时阶段使用的背景音乐
- `main-background.m4a`：从告白信开始持续循环的主背景音乐
- `main-background-video.mp4`：主背景视频参考文件

正式歌词来自 `D:\生日礼物\歌词.docx`，已经整理成 `story-config.js` 的 `lyricCues` 时间轴。网页根据 `main-background.m4a` 的实际播放时间显示歌词，暂停、继续和循环时会自动同步。

网页会按阶段自动切换音乐。祝福视频手动播放时，主背景音乐会暂时让位；视频暂停或结束后会恢复。

开场页和结尾页使用同一张裁切后的头像：

```text
assets/images/avatar-square.jpg
```

该图片由提供的竖版照片裁成 `900 × 900`，构图重点保留蜡笔小新和小白。

倒计时章节已替换为独立的 Three.js 水晶倒计时与 3D 蛋糕：

```text
countdown-3d-demo/
```

主站使用嵌入模式自动启动，倒计时完成后才显示“继续”按钮。

## 3. 来自远方一个帅气男友的一封信

你给的长文已经放在：

```text
src/data/story-config.js
```

对应字段：

```js
blessing: []
```

这一页现在放在照片前面，会一个字一个字显示。整段显示完以后才出现“继续”按钮，不提前暴露下一幕。

## 4. 爱心展示墙

爱心展示墙在告白信后面，使用网页版本复刻了本地 Python 爱心弹窗逻辑：先组成爱心，再散开。看完后点击“继续”进入照片展示。

爱心里的短句在：

```js
heartMessages: []
```

## 5. 大图胶卷相册

照片已经复制到：

```text
assets/photos/timeline-numbered/
```

当前共 45 张，已从 `D:\生日礼物\照片\照片\新建文件夹\照片` 重新同步，并按 `01.jpg` 到 `45.jpg` 的纯编号顺序展示，不显示年龄文字。

照片阶段现在包含放映机光束、扫描颗粒、持续的低音量机械声，以及换片时的翻页透视动画和快门声。

翻页方式：

- 左右按钮：上一张/下一张
- 键盘方向键：上一张/下一张
- 右箭头到最后一张后，会直接进入祝福视频

胶卷翻页声是网页实时生成的“胶片擦过 + 齿轮卡位”短音效，不需要额外音频文件。翻页时背景音乐会短暂压低后平滑恢复，保证音效更清楚又不互相打架。

## 6. 一个祝福视频

现在只保留一个视频位置：

```text
assets/videos/blessing.mp4
```

最终合并视频已放入上述位置，页面使用接近全宽的大播放器展示。视频不会自动播放；手动播放后背景音乐会平滑降低，暂停或播放结束时自动恢复。

## 7. 留言板

视频后面是只读留言板。当前内容来自 `D:\生日礼物\留言.docx`，按文档中的 6 个分类展示：家人、朋友、闺蜜、男朋友、蜡笔小新、麻圆和汤圆。同一分类中的多段文字合并在同一张卡片中，旧占位留言已全部移除。

## 8. 最后流星和彩蛋

留言板之后进入最后流星/烟花彩蛋，再进入结尾页。结尾页会持续飘落发光爱心与多色星星。当前已经使用提取好的参考资源：

```text
assets/reference/meteor-reference.mp4
assets/reference/fireworks-reference.mp4
assets/reference/main-background-video.mp4
```

开场和结尾使用 `assets/images/avatar-square.jpg`，不再使用 CSS 绘制头像或旧的 `reference-meme.png`。
