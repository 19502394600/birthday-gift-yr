# 设计分析与结构说明

## 项目定位

这是一个私人、沉浸式、电影感、互动式生日网站。新版把“来自远方一个帅气男友的一封信”提前，让她先读到你的心意，再进入爱心墙和照片回忆。

情绪曲线：

```text
密码进入 -> 流星雨开场 -> 倒计时 -> 逐字告白 -> 爱心展示墙 -> 大图胶卷相册 -> 单个视频 -> 留言板 -> 最后流星和彩蛋
```

## 当前流程

```text
Password
  -> Meteor Opening
  -> Cyber Crystal Countdown
  -> Blessing Text Typewriter
  -> Heart Wall
  -> Large Film Gallery
  -> Single Blessing Video
  -> Message Board
  -> Final Meteor / Fireworks
  -> Ending
```

## 重点交互

- 密码：当前是 `20060907`
- 开场：`小杨同学，生日快乐！` 全部显示完，才出现“开启礼物”按钮
- 倒计时：深蓝宇宙背景、青白粒子数字、FINAL、曲速飞散和冷色烟花衔接
- 阶段音乐：倒计时使用 `assets/music/countdown-background.m4a`；进入告白信后使用 `assets/music/main-background.m4a` 循环播放，视频播放时自动降低音量，暂停或结束后恢复
- 倒计时衔接：先短暂提示“你准备好了吗？”，无需点击，随后直接进入 5 到 1
- 告白后的歌词：参考主背景视频的氛围，在页面底部以低对比度字幕带循环浮现，不遮挡正文与按钮
- 头像：开场和结尾统一使用 CSS 绘制的蜡笔小新风格头像
- 告白信：长文逐字出现，全部显示完后才出现含蓄的“继续”按钮
- 爱心墙：复刻 Python 爱心弹窗，先成心形再散开
- 相册：底部缩略胶片条已删除，只显示放大版照片；不再显示年龄；最后一张才出现“继续”
- 胶卷音效：翻页时实时生成清脆的高频胶片与齿轮声，同时短暂压低背景音乐
- 视频：只保留一个大尺寸视频位置，已接入 `assets/videos/blessing.mp4`，手动播放，不自动播放
- 留言板：视频后进入，按提供文档中的 6 个分类只读展示
- 最后：留言板后进入流星/烟花彩蛋，结尾页有发光爱心与多色星星持续飘落

## 代码结构

```text
生日礼物/
├── index.html
├── styles.css
├── src/
│   ├── app.js
│   └── data/
│       ├── story-config.js
│       └── site-config.js
└── assets/
    ├── photos/
    │   └── timeline-numbered/
    ├── videos/
    ├── music/
    └── reference/
```

正式加载的是 `src/data/story-config.js` 和 `src/app.js`。`src/data/site-config.js` 是旧版配置文件，目前不参与主站渲染。

## 仍可补充的素材

- 真实留言板内容：替换 `story-config.js` 里的 `boardMessages`
